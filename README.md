# TODO
-tune ngrid values to find acceptable steering configuration
    - current behaviour is nonsense.
    - ranges are very high but no values are returning. 
    - potentially will need to do smaller more refined ranges
    - NOTE: minimum incrementation is currently disabled (not run yet)

    current calculation time is approximately an hour
